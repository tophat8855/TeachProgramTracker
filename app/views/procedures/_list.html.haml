.well.well-small
  .pull-right{ :'data-position' => 'bottom', :'data-intro' => 'Reset the filter settings to defaults.' }
    = link_to "Reset filters", reset_filterrific_url, :class => 'btn btn-default btn-sm'
  .chardin_box{ :'data-position' => 'bottom', :'data-intro' => "will_paginate's entries info works as expected." }
    = page_entries_info procedures, :model => 'procedure'
  .chardin_box{ :'data-position' => 'bottom', :'data-intro' => 'Spinner indicates update.' }
    = render_filterrific_spinner

%table.ui.celled.table
  %thead
    %tr
      %th{:colspan => "7"}
        - if @priviledged == false
          %div Your procedure history
        - else
          %div Procedure history
      %tr
        %th.collapsing
          %i.heartbeat.icon
          Procedure Type
        %th.collapsing
          %i.calendar.icon
          Date
        %th.collapsing
          %i.marker.icon
          Clinic
        %th
          %i.sticky.note.icon
          Notes
        %th.right.collapsing
          %i.user.icon
          Resident
        %th.right.collapsing
          Confirmation Status
        %th.right.collapsing
  %tbody
    %th= filterrific_sorting_link(@filterrific, :name)
    %th Procedure Name
    %th Clinic Location
    %th Registered at
  - @procedures.each do |procedure|
    %tr.procedure
      %td.name= procedure.name
      %td.email= procedure.clinic_location
      /%td.country= student.country_name
      /%td.created_at= student.created_at.to_date.to_s(:long)
  -# (@procedures.order(date: :desc)).each do |p|
/   - @procedures.each do |p|
/     - if p != nil
/       %tr
/         %td.collapsing
/           = p.name
/         %td.collapsing
/           =p.date.to_s
/         %td.collapsing 
/           =p.clinic_location
/         %td 
/           =p.notes
/         %td.right.collapsing 
/           =p.resident_name
/         %td.right.collapsing 
/           - if p.confirmation
/             %i.checkmark.box.icon
/           - else
/             %i.square.outline.icon
/         %td.right.collapsing 
/           = link_to "View", "/procedures/#{p.id}", method: :get
/           &emsp;
/           = link_to "Edit", "/procedures/#{p.id}/edit", method: :get
/           &emsp;
/           = link_to "Destroy", "/procedures/#{p.id}", method: :delete, data: { confirm: "You sure?" }

/ %table.table
/   %tr
/     %th= filterrific_sorting_link(@filterrific, :name)
/     %th Procedure Name
/     %th Clinic Location
/     %th Registered at
/   - @procedures.each do |procedure|
/     %tr.procedure
/       %td.name= procedure.name
/       %td.email= procedure.clinic_location
/       /%td.country= student.country_name
/       /%td.created_at= student.created_at.to_date.to_s(:long)
.chardin_box{ :'data-position' => 'top', :'data-intro' => "will_paginate's paginator works as expected." }
  = will_paginate @procedures, renderer: WillPaginateSemanticUi::ActionView::Renderer